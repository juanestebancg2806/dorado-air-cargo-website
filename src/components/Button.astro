---
interface Props {
  text: string;
  href: string;
  className?: string;
}
const { text, href, className } = Astro.props;
---

<a
  href={href}
  class={`p-4 font-open-sans text-lg font-bold bg-yellow-orange text-white rounded-lg hover:bg-blue-dark/80 transition ${className}`}
>
  {text}
</a>
