---
interface Props {
  text: string;
  href: string;
  className?: string;
}
const { text, href, className } = Astro.props;
---

<a href={href} class={`px-4 py-2 bg-blue-500 text-white rounded-lg hover:bg-blue-700 transition ${className}`}>
  {text}
</a>
