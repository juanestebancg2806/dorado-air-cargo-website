---
interface Props {
  text: string;
  href: string;
  className?: string;
}
const { text, href, className } = Astro.props;
---

<a
  href={href}
  class={`px-4 py-2 bg-blue-dark text-white rounded-lg hover:bg-blue-dark/80 transition ${className}`}
>
  {text}
</a>
