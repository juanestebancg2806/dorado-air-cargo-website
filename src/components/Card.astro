---
const { title, description, image, link } = Astro.props;
---

<div class="bg-white shadow-lg rounded-xl overflow-hidden p-6 text-center">
  <img src={image} alt={title} class="w-full h-40 object-cover rounded-md" />
  <h2 class="text-xl font-bold mt-4">{title}</h2>
  <p class="text-gray-500 mt-2">{description}</p>
  <a href={link} class="mt-4 inline-block bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-6 rounded-lg transition">
    Ver más
  </a>
</div>
