---
import MainLayout from "../layouts/MainLayout.astro";

// Lista de siglas con su explicación descripcion
const siglas = [
  {
    sigla: "FOB",
    descripcion: "Free On Board",
    imagen: "/fob.png",
  },
  {
    sigla: "B2B",
    descripcion: "Business to Business",
    imagen: "/b2b.png",
  },
  {
    sigla: "AWB",
    descripcion: "Air Waybill",
    imagen: "/awb.png",
  },
  {
    sigla: "C/O",
    descripcion: "Certificate Origin",
    imagen: "/co.png",
  },
  {
    sigla: "CRM",
    descripcion: "Customer Relationship Management",
    imagen: "/crm.png",
  },
  {
    sigla: "INCOTERMS",
    descripcion: "International Commercial Terms",
    imagen: "/incoterms.png",
  },
  {
    sigla: "EDD",
    descripcion: "Expected Delivery Date",
    imagen: "/edd.png",
  },
];
---

<MainLayout metaTitle="Sobre Nosotros" metaDescription="Sobre Nosotros">
    <!-- Banner - Sobre Nosotros -->
    <section
    class="bg-blue-dark shadow-sm p-6 md:p-10 mb-14 md:mb-20 flex flex-col md:flex-row items-center gap-6 font-open-sans min-h-52 justify-center md:justify-start"
    >
    <div class="md:ml-10 text-justify">
        <h1 class="text-3xl md:text-4xl font-bold text-white mb-2">
        Sobre Nosotros
        </h1>
        <p class="text-white max-w-xl">
        En El Dorado Air Cargo construimos confianza desde hace más de 30 años. Nuestro equipo combina experiencia, compromiso y visión para ofrecer soluciones logísticas que conectan negocios con el mundo.
        </p>
    </div>
    </section>

    <!-- Mapa con contexto -->
    <section class="w-full py-12 px-6 md:px-12 bg-gray-100 font-open-sans">
        <div class="max-w-6xl mx-auto text-center mb-8">
        <h2 class="text-2xl md:text-3xl font-bold text-blue-dark mb-4">
            Conexiones globales que cruzan fronteras
        </h2>
        <p class="text-lg text-gray-700">
            Nuestro mapa refleja la presencia internacional de El Dorado Air Cargo. Cada marca indica los países desde donde exportamos o importamos mercancía para nuestros clientes.
        </p>
        </div>
    
        <!-- Imagen principal -->
        <div class="flex justify-center">
        <img
            src="/mapamundi.png"
            alt="Mapamundi"
            class="w-full max-w-6xl h-auto rounded-lg shadow-lg object-contain"
        />
        </div>
    </section>
  

    <!-- Sección de acronimos -->
    <section class="py-16 px-6 md:px-12 bg-gray-50 font-open-sans">
    <div class="text-center mb-12">
        <h2 class="text-3xl md:text-4xl font-bold text-blue-dark mb-4">Acrónimos</h2>
        <p class="text-lg text-gray-600 max-w-3xl mx-auto">
        Acrónimos de comercio exterior que más utilizamos
        </p>
    </div>

    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8 justify-items-center">
        {siglas.map(({ sigla, descripcion, imagen }, index) => (
        <div
            class="bg-white rounded-xl shadow-lg p-6 flex flex-col items-center text-center transition-transform duration-700 hover:scale-105 opacity-0 translate-y-10 animate-fade-in-up"
            style={`animation-delay: ${index * 0.2}s`}
        >
            <img src={imagen} alt={sigla} class="w-24 h-24 object-cover rounded-full mb-4" />
            <h3 class="text-4xl font-extrabold text-blue-dark">{sigla}</h3>
            <p class="text-gray-600 mt-2 text-sm">{descripcion}</p>
        </div>
        ))}
    </div>
    </section>

  <style>
    @keyframes fade-in-up {
      0% {
        opacity: 0;
        transform: translateY(30px);
      }
      100% {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .animate-fade-in-up {
      animation: fade-in-up 0.8s ease-out forwards;
    }
  </style>
</MainLayout>
